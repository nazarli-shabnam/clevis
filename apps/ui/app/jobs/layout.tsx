export const dynamic = "force-dynamic"
export const metadata = { title: "Job Queue · clevis" }
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
