export const dynamic = "force-dynamic"
export const metadata = { title: "Health & Security · clevis" }
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
